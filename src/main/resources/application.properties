spring.application.name=lab13-daw

# Configuración de la base de datos (usa variables de entorno si están disponibles, sino usa MySQL local)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://db:3306/example_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:rootpassword}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER:com.mysql.cj.jdbc.Driver}

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_DIALECT:org.hibernate.dialect.MySQLDialect}
spring.jpa.properties.hibernate.format_sql=true

# Puerto del servidor (Render asigna el puerto automáticamente)
server.port=${PORT:8080}
